{% extends "layouts/layout-dashboard.twig" %}
         {% set page_group = "dashboard" %}

         {% block page %}
             {% set page = page | merge({
                 "title"       : "Shipments",
                 "description" : "A list of your Shipments."
             }) %}
             {{ parent() }}
         {% endblock %}
         {% block page_scripts %}
            <script>
            $(document).ready(function() {
                // Load the validator rules for this form
                var validators = {{validators | raw}};
                ufFormSubmit(
                  $("form[name='trackingNumber']"),
                  validators,
                  $("#userfrosting-alerts"),
                  function(data, statusText, jqXHR) {
                      // Reload the page on success
                      window.location.reload(true);
                  }
                );
            });
        </script>
        {% endblock %}
         {% block content %}
         <h1>Your Shipments:</h1>
         <br>



         <table>
        <tr style="font-weight:bold;font-size:15px">
            <td style= "padding-bottom:10px;">#</td>
            <td style="padding-bottom:10px;padding-left:20px;">Tracking Number</td>
            <td style="padding-bottom:10px;padding-left:20px;">Shipment Date</td>
            <td style="padding-bottom:10px;padding-left:20px;">Shipment Origin</td>
            <td style="padding-bottom:10px;padding-left:20px;">Shipment Destination</td>
            <td style="padding-bottom:10px;padding-left:20px;">Current Location</td>
            <td style="padding-bottom:10px;padding-left:20px;">Estimated Time of Arrival</td>
        </tr>
        {% set increment = 1 %}
        {% if trackingNumbers|length == 0 %}
        <div class="form-group">You have no tracking numbers</div>
        {% else %}
         {% for trackingNumber in trackingNumbers %}
         <tr>
             <td>{{increment}}.</td>
             <td style="padding-left:20px">{{trackingNumber.trackingNumber}}</td>
             <td style="padding-left:20px"><script>var dickbutt ={{trackingNumber.shipDate}};dickbutt=dickbutt.toString();document.write(dickbutt.substring(4,6) + "/" + dickbutt.substring(6,8)+ "/" + dickbutt.substring(0,4));</script></td>
             <td style="padding-left:20px">{{trackingNumber.origin}}</td>
             <td style="padding-left:20px">{{trackingNumber.destination}}</td>
             <td style="padding-left:20px">{{trackingNumber.currentLocation}}</td>
             <td style="padding-left:20px"><script>var dickbutt ={{trackingNumber.eta}};dickbutt=dickbutt.toString();document.write(dickbutt.substring(4,6) + "/" + dickbutt.substring(6,8)+ "/" + dickbutt.substring(0,4));</script></td>
        </tr>
        {% set increment = increment + 1 %}
         {% endfor %}
        </table>
     {% endif %}
        <br><br><br>
        <!-- <form class="form-horizontal" role="form" name="trackingNumber" action="{{site.uri.public}}/shipments" method="post">
                   <div class="form-group">
                     <label for="trackingnumber">Add Tracking Number:</label>
                   </div>
                   <div class="form-group">
                       <input type="text" class="form-control" id="trackingnumber" name = "trackingNumber" style="width:300px">
                    </div>
   <div class="form-group">
             <button type="submit" class="btn btn-success text-center">Submit</button>
         </div>
       </form> -->
        <br><br><br><br><br><br><br><br><br><br><br><br><br><br>

        {% endblock %}
